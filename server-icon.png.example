# This is a placeholder file to prevent Docker build failures
# 
# To add a custom server icon:
# 1. Create a 64x64 PNG image
# 2. Name it "server-icon.png" (without the .example extension)
# 3. Place it in the project root directory
# 4. Rebuild your Docker image
#
# Alternatively, you can pass SERVER_ICON_URL as a build argument:
# docker build --build-arg SERVER_ICON_URL="https://example.com/icon.png" -f Dockerfile.dev .
